{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "npm start",
            "name": "Start dev",
            "cwd": "${workspaceRoot}",
            "request": "launch",
            "type": "node-terminal"
        },
    ]
}