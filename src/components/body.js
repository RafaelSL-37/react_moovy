import logo from '../logo.svg';

export default function Body() {
    return (
        <div>
            THIS IS A BODY
            <img src={logo} className="App-logo" alt="logo" />
        </div>
    );
}