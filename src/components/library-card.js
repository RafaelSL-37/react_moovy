const libraryStyle = {
    backgroundColor: 'green',
    fontColor: 'black',
    margin: '2px',
}

export default function LibraryCard() {
    return (
        <h1 style={libraryStyle}>
            THIS IS A CARD
        </h1>
    );
}